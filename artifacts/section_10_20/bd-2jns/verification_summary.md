# bd-2jns - Maintainer/publisher fragility model + SPOF detection

## Verdict

`PASS_STATIC_ONLY` pending the required `rch` Rust proof (full PASS once the
`cargo test --test dgis_fragility_spof` line below runs green).

This file replaces the stale 2026-02-22 placeholder that the bd-8vo8v audit
flagged as fabricated evidence; the JSON sibling
`verification_evidence.json` is now machine-generated by
`scripts/check_fragility_spof.py`.

## Gate

`scripts/check_fragility_spof.py` (~470 LOC) verifies the real bd-2jns
implementation surface and the 10 on-disk fragility fixtures:

- `crates/franken-node/src/dgis/fragility_model.rs` (839 LOC, 27 `#[test]`)
- `crates/franken-node/src/dgis/spof_detection.rs` (1347 LOC, 17 `#[test]`)
- `crates/franken-node/src/dgis/fragility_fixtures.rs` (1045 LOC, 15 `#[test]`)
- `tests/security/dgis_fragility_spof.rs` (394 LOC, 12 `#[test]`)
- `tests/security/fragility_fixtures/{5 SPOF + 5 robust}.json`
- `crates/franken-node/Cargo.toml` (`[[test]] name = "dgis_fragility_spof"`)

## Sub-task chain (bd-2jns.1, 5/5 complete)

| ST  | Commit       | Surface                                                     | Tests |
| --- | ------------ | ----------------------------------------------------------- | ----- |
| 1   | `87dbad2a`   | `src/dgis/fragility_model.rs`                               | 27    |
| 2   | `1ccf78ba`   | `src/dgis/spof_detection.rs`                                | 17    |
| 3   | `802350e9`   | `src/dgis/fragility_fixtures.rs` + 10 fixtures              | 15    |
| 4   | `caf223cc`   | `tests/security/dgis_fragility_spof.rs` integration test    | 12    |
| 5   | *this*       | `scripts/check_fragility_spof.py` verification gate         |  -    |

Cumulative test coverage: **71 tests** (27 + 17 + 15 + 12).

## Static evidence (6/6 checks)

1. `fixture_directory` — directory contains exactly the 10 expected JSON files.
2. `fixture_schemas` — every fixture parses, declares the required schema
   fields, and respects the SPOF/robust class invariants.
3. `fragility_model_source` — LOC > 500, has `assess_maintainer` +
   `assess_publisher`, has `#[test]` blocks.
4. `spof_detection_source` — LOC > 800, has `detect_spofs`, has `#[test]`
   blocks.
5. `integration_test_and_cargo` — all 12 required `#[test]` functions exist
   AND the test is registered in `Cargo.toml`.
6. `cargo_dgis_fragility_spof` — placeholder when invoked with `--skip-cargo`;
   when run normally it executes the integration test through rch.

## Required full proof

```bash
rch exec -- env CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=1 \
  CARGO_TARGET_DIR=/data/tmp/franken_node-crimsoncrane-bd2jns-target \
  cargo test -p frankenengine-node --test dgis_fragility_spof -- --nocapture
```

All 12 integration tests in `tests/security/dgis_fragility_spof.rs` must
report `test result: ok` for the gate verdict to flip from
`PASS_STATIC_ONLY` to `PASS`.
