[package]
name = "supervision-kernel-harness"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
syn = { version = "2.0", features = ["full"] }
