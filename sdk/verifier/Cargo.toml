[package]
name = "frankenengine-verifier-sdk"
version = "0.1.0"
edition = "2024"
description = "Universal verifier SDK for replaying signed capsules and reproducing claim verdicts"
license = "MIT"

[dependencies]
sha2 = "0.10.8"
hex = "0.4.3"
subtle = "2.6.1"

[dev-dependencies]
